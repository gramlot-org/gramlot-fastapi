#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
git diff --cached --check
uv run --frozen --extra dev --extra docs ruff check .
if ! uv run --frozen --extra dev --extra docs mypy src/; then
    echo "mypy reported advisory findings."
fi
