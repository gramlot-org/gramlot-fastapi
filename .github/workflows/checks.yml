name: Checks
on:
  push:
    branches: [main, develop, 'codex/**', 'wf/**']
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        python: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Install development dependencies
        run: python -m pip install '.[dev,docs]'
      - name: Lint, available tests and documentation
        run: python scripts/check.py
      - name: Build and validate distributions
        run: |
          python -m build
          python -m twine check dist/*
      - name: Verify installed wheel outside the checkout
        run: |
          python -m venv "$RUNNER_TEMP/wheel-check"
          "$RUNNER_TEMP/wheel-check/bin/python" -m pip install --no-deps dist/*.whl
          cd "$RUNNER_TEMP"
          "$RUNNER_TEMP/wheel-check/bin/python" -I -c 'import gramlot_fastapi; from importlib.metadata import version; print(version("gramlot-fastapi"))'
